<nav class="navbar navbar-expand-lg border-bottom p-3 mb-3 bg-white">
    <div class="container gap-3">
        <a class="navbar-brand me-4" href="{{ path('app_home') }}" data-turbo="false">
            <img src="{{ asset('images/logo.png') }}" alt="Logo" width="100" height="45" class="d-inline-block align-text-top" />
        </a>

        <div class="collapse navbar-collapse gap-lg-4" id="mainNavbar">
            <ul class="navbar-nav ms-auto align-items-lg-center gap-2 gap-lg-4 mb-3 mb-lg-0">
                <li class="nav-item">
                    <a href="{{ path('app_covoiturage_new') }}" class="btn btn-vert w-100 w-lg-auto" data-turbo="false">Proposer un trajet</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_contact') }}" class="nav-link link-body-emphasis" data-turbo="false">Contact</a>
                </li>
            </ul>
        </div>

        <div class="d-flex align-items-center">
            <button class="navbar-toggler me-3" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            {% if app.user %}
                <div class="d-flex align-items-center gap-3">
                    <span class="btn-details py-2 px-3" style="line-height:1;">
                        {{ app.user.creditBalance }} credits
                    </span>
                    <div class="dropdown text-end">
                        <a href="#" class="d-block link-body-emphasis text-decoration-none dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-person-circle fs-4"></i>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end text-small shadow">
                            {% if is_granted('ROLE_ADMIN') %}
                                <li><a class="dropdown-item" href="{{ path('app_dashboard') }}" data-turbo="false">Dashboard</a></li>
                                <li><hr class="dropdown-divider"></li>
                            {% endif %}
                            <li><a class="dropdown-item" href="{{ path('app_profile_trips') }}" data-turbo="false">Mes trajets</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_profile') }}" data-turbo="false">Profil</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('app_logout') }}" data-turbo="false">Deconnexion</a></li>
                        </ul>
                    </div>
                </div>
            {% else %}
                <div class="d-flex align-items-center gap-3">
                    <a href="{{ path('app_register') }}" class="nav-link link-body-emphasis" data-turbo="false">Inscription</a>
                    <a href="{{ path('app_login') }}" class="nav-link link-body-emphasis" data-turbo="false">Connexion</a>
                </div>
            {% endif %}
        </div>
    </div>
</nav>
